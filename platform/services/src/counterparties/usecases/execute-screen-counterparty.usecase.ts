/**
 * ExecuteScreenCounterparty - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: counterparties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteScreenCounterparty Use Case
 *
 * DDD: Application use case for get Screen.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { ScreenCounterpartyInput, ScreenCounterpartyOutput } from "../dto/screen.dto";
import type { ExecutionContextService, IdGeneratorService } from "@provenara/services/_shared/index.js";
import type { ScreenRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteScreenCounterparty {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly screen: ScreenRepository
  ) {}

  async execute(input: ScreenCounterpartyInput): Promise<ScreenCounterpartyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctpId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).counterpartyId) {
      throw new ValidationError("counterpartyId is required");
    }
    const counterpartyId = (input as any).counterpartyId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.screen.screenCounterparty({ counterpartyId: counterpartyId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${counterpartyId}`);
    }

    return entity as ScreenCounterpartyOutput;
  }
}
