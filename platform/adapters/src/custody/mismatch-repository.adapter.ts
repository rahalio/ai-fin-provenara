/**
 * MismatchRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: custody
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * MismatchRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements MismatchRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  MismatchRepository,
} from "@provenara/services/custody";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { MismatchRepositoryDdb } from "./mismatch-repository.ddb.js";

/**
 * MismatchRepositoryAdapter
 *
 * Implements MismatchRepository port for infrastructure layer.
 */
export class MismatchRepositoryAdapter implements MismatchRepository {
  private readonly ddb: MismatchRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new MismatchRepositoryDdb(this.dynamoClient);
  }

  async flagCustodyMismatch(input: Parameters<MismatchRepository['flagCustodyMismatch']>[0]): Promise<Awaited<ReturnType<MismatchRepository['flagCustodyMismatch']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.flagCustodyMismatch(input);
  }

  async clearCustodyMismatch(input: Parameters<MismatchRepository['clearCustodyMismatch']>[0]): Promise<Awaited<ReturnType<MismatchRepository['clearCustodyMismatch']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.clearCustodyMismatch(input);
  }
}
