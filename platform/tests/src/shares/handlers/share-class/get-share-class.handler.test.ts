/**
 * GetShareClass handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: shares
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getShareClass } from "@provenara/api-server/domains/shares/handlers";
import type { SharesDDDDependencies } from "@provenara/api-server/domains/shares";
import { createShareClass as createShareClassEntity } from "@provenara/factories/shares";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@provenara/tests/shared";
import { SHARES_USE_CASES } from "../__factories__/use-cases.js";

describe("GetShareClass", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: SharesDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<SharesDDDDependencies>(
      SHARES_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createShareClassEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetShareClass, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { shareClassId: entity.id };

    await getShareClass(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetShareClass.execute).toHaveBeenCalledWith(expect.objectContaining({ shareClassId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { shareClassId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetShareClass, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getShareClass(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
