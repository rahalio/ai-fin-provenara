/**
 * GetTitleStatus handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: title
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getTitleStatus } from "@provenara/api-server/domains/title/handlers";
import type { TitleDDDDependencies } from "@provenara/api-server/domains/title";
import { createTitle as createTitleEntity } from "@provenara/factories/title";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@provenara/tests/shared";
import { TITLE_USE_CASES } from "../__factories__/use-cases.js";

describe("GetTitleStatus", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TitleDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TitleDDDDependencies>(
      TITLE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createTitleEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetTitleStatus, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { artworkId: entity.id };

    await getTitleStatus(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetTitleStatus.execute).toHaveBeenCalledWith(expect.objectContaining({ artworkId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { artworkId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetTitleStatus, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getTitleStatus(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
