/**
 * ExecuteUpsertTitleStatus - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: title
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteUpsertTitleStatus Use Case
 *
 * DDD: Application use case for get Title.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { UpsertTitleStatusInput, UpsertTitleStatusOutput } from "../dto/title.dto";
import type { ExecutionContextService, IdGeneratorService } from "@provenara/services/_shared/index.js";
import type { TitleRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteUpsertTitleStatus {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly title: TitleRepository
  ) {}

  async execute(input: UpsertTitleStatusInput): Promise<UpsertTitleStatusOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ttlId();

    // All field names extracted from OpenAPI spec: notes, status
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).artworkId) {
      throw new ValidationError("artworkId is required");
    }
    const artworkId = (input as any).artworkId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.title.upsertTitleStatus({ artworkId: artworkId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${artworkId}`);
    }

    return entity as UpsertTitleStatusOutput;
  }
}
