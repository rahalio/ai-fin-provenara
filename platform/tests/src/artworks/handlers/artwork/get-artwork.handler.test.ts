/**
 * GetArtwork handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: artworks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getArtwork } from "@provenara/api-server/domains/artworks/handlers";
import type { ArtworksDDDDependencies } from "@provenara/api-server/domains/artworks";
import { createArtwork as createArtworkEntity } from "@provenara/factories/artworks";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@provenara/tests/shared";
import { ARTWORKS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetArtwork", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: ArtworksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<ArtworksDDDDependencies>(
      ARTWORKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createArtworkEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetArtwork, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { artworkId: entity.id };

    await getArtwork(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetArtwork.execute).toHaveBeenCalledWith(expect.objectContaining({ artworkId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { artworkId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetArtwork, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getArtwork(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
