/**
 * TitleRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: title
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * TitleRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements TitleRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  TitleRepository,
} from "@provenara/services/title";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { TitleRepositoryDdb } from "./title-repository.ddb.js";

/**
 * TitleRepositoryAdapter
 *
 * Implements TitleRepository port for infrastructure layer.
 */
export class TitleRepositoryAdapter implements TitleRepository {
  private readonly ddb: TitleRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new TitleRepositoryDdb(this.dynamoClient);
  }

  async getTitleStatus(input: Parameters<TitleRepository['getTitleStatus']>[0]): Promise<Awaited<ReturnType<TitleRepository['getTitleStatus']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getTitleStatus(input);
  }

  async upsertTitleStatus(input: Parameters<TitleRepository['upsertTitleStatus']>[0]): Promise<Awaited<ReturnType<TitleRepository['upsertTitleStatus']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.upsertTitleStatus(input);
  }
}
