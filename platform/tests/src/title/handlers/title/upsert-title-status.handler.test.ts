/**
 * UpsertTitleStatus handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: title
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { upsertTitleStatus } from "@provenara/api-server/domains/title/handlers";
import type { TitleDDDDependencies } from "@provenara/api-server/domains/title";
import { createTitle as createTitleEntity } from "@provenara/factories/title";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@provenara/tests/shared";
import { TITLE_USE_CASES } from "../__factories__/use-cases.js";

describe("UpsertTitleStatus", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: TitleDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<TitleDDDDependencies>(
      TITLE_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with updated entity", async () => {
    const entity = createTitleEntity({ orgId });
    const input = {} as any;
    const updatedEntity = { ...entity, ...input };
    const useCaseResult = { data: updatedEntity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    mockRequest.params = { artworkId: entity.id };
    mockRequest.body = input;
    vi.spyOn(mockDeps.executeUpsertTitleStatus, "execute").mockResolvedValue(useCaseResult);

    await upsertTitleStatus(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeUpsertTitleStatus.execute).toHaveBeenCalledWith(expect.objectContaining({ artworkId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

});
