/**
 * ExecuteAttestCustody - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: custody
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteAttestCustody Use Case
 *
 * DDD: Application use case for get Custody.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { AttestCustodyInput, AttestCustodyOutput } from "../dto/custody.dto";
import type { ExecutionContextService, IdGeneratorService } from "@provenara/services/_shared/index.js";
import type { CustodyRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteAttestCustody {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly custody: CustodyRepository
  ) {}

  async execute(input: AttestCustodyInput): Promise<AttestCustodyOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.cusId();

    // All field names extracted from OpenAPI spec: eventType, notes, physicalBindingAttested
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).artworkId) {
      throw new ValidationError("artworkId is required");
    }
    const artworkId = (input as any).artworkId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.custody.attestCustody({ artworkId: artworkId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${artworkId}`);
    }

    return entity as AttestCustodyOutput;
  }
}
