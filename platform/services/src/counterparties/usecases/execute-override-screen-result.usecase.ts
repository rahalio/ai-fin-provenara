/**
 * ExecuteOverrideScreenResult - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: counterparties
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteOverrideScreenResult Use Case
 *
 * DDD: Application use case for get Override.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { OverrideScreenResultInput, OverrideScreenResultOutput } from "../dto/override.dto";
import type { ExecutionContextService, IdGeneratorService } from "@provenara/services/_shared/index.js";
import type { OverrideRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteOverrideScreenResult {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly override: OverrideRepository
  ) {}

  async execute(input: OverrideScreenResultInput): Promise<OverrideScreenResultOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.ctpId();

    // All field names extracted from OpenAPI spec: approverRef, reason
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).counterpartyId) {
      throw new ValidationError("counterpartyId is required");
    }
    const counterpartyId = (input as any).counterpartyId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.override.overrideScreenResult({ counterpartyId: counterpartyId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${counterpartyId}`);
    }

    return entity as OverrideScreenResultOutput;
  }
}
