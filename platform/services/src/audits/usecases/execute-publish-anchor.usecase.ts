/**
 * ExecutePublishAnchor - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: audits
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecutePublishAnchor Use Case
 *
 * DDD: Application use case for get Anchor.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { PublishAnchorInput, PublishAnchorOutput } from "../dto/anchor.dto";
import type { ExecutionContextService, IdGeneratorService } from "@provenara/services/_shared/index.js";
import type { AnchorPublisher, AnchorRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecutePublishAnchor {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly anchorRepository: AnchorRepository,
    private readonly anchorPublisher: AnchorPublisher
  ) {}

  async execute(input: PublishAnchorInput): Promise<PublishAnchorOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.audId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).artworkId) {
      throw new ValidationError("artworkId is required");
    }
    const artworkId = (input as any).artworkId;

    // Publish via publisher port (pass correlation ID for response meta)
    const entity = await this.anchorPublisher.publishAnchor({ artworkId: artworkId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${artworkId}`);
    }

    return entity as PublishAnchorOutput;
  }
}
